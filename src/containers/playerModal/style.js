import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  modalContainer: {
    flex: 1,
    justifyContent: 'flex-end'
  },
  mainConatiner: {
    flex: 0.5,
    backgroundColor: '#fff',
    borderRadius: 15
  }
});
